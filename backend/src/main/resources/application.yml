server:
  port: 8080

spring:
  datasource:
    url: "jdbc:postgresql://db.vuuezzibhsltyfaoscgu.supabase.co:5432/postgres"
    username: "postgres"
    password: "3cFEPz2pSrgMQM9"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  jackson:
    serialization:
      write-dates-as-timestamps: false
    date-format: yyyy-MM-dd'T'HH:mm:ss

app:
  jwt:
    secret: "aW50ZWdyYWRvLWRlZmVuc2FzLXVyYmUtand0LXNlY3JldC1rZXktMjAyNi1lbnRlcnByaXNl"
    expiration-ms: 86400000

  scheduler:
    consumir-institucional: "0 0 6 * * ?"

logging:
  level:
    com.urbe.defensas: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
